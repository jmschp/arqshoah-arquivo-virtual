<div class="container">
  <%= render "shared/show_page_header", record: @savior %>

  <div class="row mt-5 mb-5">
    <div class="col-12 col-md-6">
      <%= render "shared/show_page_carousel", record: @savior %>
    </div>

    <div class="col-12 col-md-6">
      <%= render "shared/show_page_table", record: @savior %>
    </div>
  </div>

  <div class="row">
    <div class="accordion" id="accordion-panels">
      <%= render "shared/accordion_items/professional_activities", { record: @savior } %>
      <%= render "shared/accordion_items/rich_text", { record: @savior, rich_text_name: :description } %>
      <%= render "shared/accordion_items/rich_text", { record: @savior, rich_text_name: :observation } %>
    </div>
  </div>
</div>
