<div class="container">
  <%= render "shared/show_page_header", record: @iconography %>

  <div class="row mt-5 mb-5">
    <div class="col-12 col-md-6">
      <%= render "shared/show_page_carousel", record: @iconography %>
    </div>

    <div class="col-12 col-md-6">
      <%= render "shared/show_page_table", record: @iconography %>
    </div>
  </div>

  <div class="row">
    <div class="accordion" id="accordion-panels">
      <%= render "shared/accordion_items/citations", { record: @iconography } %>
      <%= render "shared/accordion_items/rich_text", { record: @iconography, rich_text_name: :description } %>
      <%= render "shared/accordion_items/rich_text", { record: @iconography, rich_text_name: :observation } %>
    </div>
  </div>
</div>
